# Coleções

\`\`\`python
lista = [1, 2]
lista.append(3)
lista.pop()
lista[0]
lista[:]

tupla = (1, 2)
a, b = tupla

dado = {"nome": "Ana"}
dado.get("email")
dado["nome"] = "Bia"
for chave, valor in dado.items():
    pass

conjunto = {"a", "b"}
conjunto.add("c")
conjunto_a & conjunto_b
conjunto_a | conjunto_b
\`\`\`

- \`list\`: ordem/mutável.
- \`tuple\`: sequência imutável.
- \`dict\`: chave → valor.
- \`set\`: elementos únicos.

